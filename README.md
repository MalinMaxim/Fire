# Background Animation Website

Современный веб-сайт с анимированным видео-фоном и интерактивным слайдером. Видео синхронизируется с переключением слайдов, создавая плавный визуальный эффект.

![Preview](media/background.mp4)

## 🚀 Особенности

- **Видео-фон**: Анимированный видео-фон, синхронизированный с переключением слайдов
- **Слайдер**: Интерактивный слайдер с навигацией и пагинацией
- **Анимации**: Плавные переходы с использованием GSAP
- **Адаптивный дизайн**: Респонсивная верстка с использованием CSS переменных
- **Современный UI**: Минималистичный дизайн с акцентом на контент

## 📁 Структура проекта

```
background-animation-website/
├── index.html              # Главная страница
├── css/
│   └── main.css            # Основные стили
├── js/
│   └── app.js              # JavaScript логика
├── img/
│   └── logo.svg            # Логотип
├── libs/
│   ├── gsap/
│   │   └── gsap.min.js     # GSAP библиотека для анимаций
│   └── swiper/
│       ├── swiper-bundle.min.css
│       └── swiper-bundle.min.js
├── media/
│   └── background.mp4      # Видео фон
└── src/                    # Исходные файлы
    ├── background-60.mp4
    ├── background-src.mp4
    ├── command.txt
    └── logo.svg
```

## 🛠️ Технологии

- **HTML5** - Разметка
- **CSS3** - Стилизация с использованием CSS переменных
- **JavaScript** - Интерактивность
- **GSAP** - Библиотека для плавных анимаций
- **Swiper.js** - Современный слайдер
- **FFmpeg** - Обработка видео (для исходных файлов)

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/background-animation-website.git
```

2. Перейдите в директорию проекта:
```bash
cd background-animation-website
```

3. Откройте `index.html` в браузере или используйте локальный сервер:

### Использование локального сервера (рекомендуется)

**Python:**
```bash
python -m http.server 8000
```

**Node.js (http-server):**
```bash
npx http-server
```

**PHP:**
```bash
php -S localhost:8000
```

Затем откройте браузер и перейдите по адресу `http://localhost:8000`

## 🎬 Использование

### Основные элементы управления:

- **Мышь**: Скролл для переключения слайдов
- **Навигационные стрелки**: Кнопки "Предыдущий/Следующий" внизу справа
- **Пагинация**: Точки внизу для прямого перехода к слайду

### Кастомизация

#### Изменение количества слайдов

Отредактируйте `index.html`, добавив или удалив блоки `.swiper-slide`:

```html
<div class="swiper-slide slide">
    <div class="slide__content">
        <h2>Ваш заголовок</h2>
        <p>Ваш текст</p>
        <button>Кнопка</button>
    </div>
</div>
```

#### Изменение стилей

Все основные параметры находятся в `css/main.css` в секции `:root`:

```css
:root {
    --index: calc(1vw + 1vh);
    --swiper-theme-color: #4b4d53;
    --swiper-navigation-size: 30px;
    /* ... */
}
```

#### Замена видео-фона

1. Поместите ваше видео в папку `media/`
2. Переименуйте его в `background.mp4` или измените путь в `index.html`:

```html
<video src="media/your-video.mp4" class="video-background" muted></video>
```

**Рекомендации для видео:**
- Формат: MP4 (H.264)
- Разрешение: 1920x1080 или выше
- Оптимизация для веб (см. секцию "Обработка видео")

## 🎥 Обработка видео

Для оптимизации видео используйте FFmpeg. Команда находится в `src/command.txt`:

```bash
ffmpeg -i background-60.mp4 -vf scale=1920:-1 -movflags faststart -vcodec libx264 -crf 20 -g 1 -pix_fmt yuv420p background.mp4
```

Параметры:
- `scale=1920:-1` - масштабирование до ширины 1920px с сохранением пропорций
- `movflags faststart` - оптимизация для веб-воспроизведения
- `crf 20` - качество (чем меньше, тем лучше качество, но больше размер)
- `pix_fmt yuv420p` - совместимость с браузерами

## 📱 Совместимость

- ✅ Chrome (последние версии)
- ✅ Firefox (последние версии)
- ✅ Safari (последние версии)
- ✅ Edge (последние версии)

## 🔧 Настройка производительности

Если видео загружается медленно:

1. Используйте оптимизированное видео (см. "Обработка видео")
2. Добавьте атрибут `preload="metadata"` к тегу `<video>` для ленивой загрузки
3. Используйте видео меньшего разрешения для мобильных устройств (можно добавить медиа-запросы)

## 📝 Лицензия

Этот проект создан для личного использования. Вы можете свободно использовать и модифицировать код.

## 👤 Автор

**MaxiMalin**

## 🙏 Благодарности

- [GSAP](https://greensock.com/gsap/) - за отличную библиотеку анимаций
- [Swiper](https://swiperjs.com/) - за мощный слайдер

---

⭐ Если вам понравился проект, поставьте звезду!

